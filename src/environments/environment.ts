// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase:{
    apiKey: "AIzaSyCDtdsadIXnD1xptZAY4aECXE-39f-qDNo",
    authDomain: "back-pdm.firebaseapp.com",
    projectId: "back-pdm",
    storageBucket: "back-pdm.appspot.com",
    messagingSenderId: "799015560851",
    appId: "1:799015560851:web:c1c45b8d829a339aec8f66",
    measurementId: "G-MYKMYQHKLE"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
